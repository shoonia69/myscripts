services:
  portal:
    build: .
    container_name: three-x-ui-portal
    restart: unless-stopped
    env_file:
      - ${ENV_FILE:-.env}
    ports:
      - "8080:8080"
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp:size=16m,mode=1777
